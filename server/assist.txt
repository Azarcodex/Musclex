if (
  order.couponApplied &&
  remainingSubtotal < order.minPurchaseforCoupon
) {
  const couponDiscountForItem =
    (itemTotal * order.couponValue) / 100;

  refundAmount = itemTotal - couponDiscountForItem;

  order.couponApplied = false;
  order.couponCode = null;
}
